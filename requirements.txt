matplotlib==3.5.1  # Update to a compatible version with Python 3.10
numpy>=1.21.0  # Use a version compatible with Python 3.10
opencv-python>=4.5.4  # Update to a compatible version with Python 3.10
Pillow>=8.4.0  # Update to a compatible version with Python 3.10
protobuf==3.20.0  # Fix version to ensure compatibility
pycocotools==2.0.2  # Use a specific compatible version
pyparsing==3.0.7  # Update to a compatible version with Python 3.10
requests>=2.26.0  # Use a version compatible with Python 3.10
rsa==4.8  # Update to a compatible version with Python 3.10
scipy>=1.7.3  # Use a version compatible with Python 3.10
six==1.16.0  # Update to a compatible version with Python 3.10
tensorboard==2.7.0  # Update to a compatible version with Python 3.10
tensorboard-plugin-wit==1.8.0  # Update to a compatible version with Python 3.10
torch==1.10.0  # Use a version compatible with Python 3.10
torchvision==0.11.1  # Update to a compatible version with Python 3.10
tqdm>=4.62.3  # Use a version compatible with Python 3.10
urllib3>=1.26.7  # Use a version compatible with Python 3.10
Werkzeug==2.1.5  # Update to a compatible version with Python 3.10
zipp==3.6.0  # Update to a compatible version with Python 3.10

# Logging -------------------------------------
tensorboard>=2.7.0  # Update to a compatible version with Python 3.10
# wandb

# Plotting ------------------------------------
pandas>=1.3.4  # Use a version compatible with Python 3.10
seaborn>=0.11.2  # Use a version compatible with Python 3.10
